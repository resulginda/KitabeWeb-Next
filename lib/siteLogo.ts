import type { StaticImageData } from 'next/image';
import logoHeader from '../public/logo-header.webp';

/** Header logosu — `/_next/static/media/*` ile immutable önbellek */
export const siteLogoHeader: StaticImageData = logoHeader;
